
<div class="homepage__wrapper">
   <%= render "partials/header" %>

   <ul class="releases__list">
      <% @releases.each do |release| %>
         <li class="releases__item">
            <%= render partial: "partials/release", locals: { release: release } %>
         </li>
      <% end %>
   </ul>
</div>
